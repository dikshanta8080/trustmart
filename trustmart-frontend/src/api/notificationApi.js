// Frontend-only notification system.
// This file exists to keep a stable integration point if backend is added later.

const notificationApi = {
  // In-memory/localStorage is handled inside NotificationContext.
};

export default notificationApi;

