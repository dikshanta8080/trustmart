spring:
  profiles:
    active: dev
  application:
    name: trustmart
server:
  port: 8080
  servlet:
    context-path: /api/v1
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB


