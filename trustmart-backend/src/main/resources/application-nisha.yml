spring:
  mail:
    port: 587
    username: dikshantaacharya04@gmail.com
    password: lhfz qiuy qhzv houl
    host: smtp.gmail.com
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  datasource:
    url: jdbc:postgresql://localhost:5432/trustmart
    username: postgres
    password: 1234

    hikari:
      pool-name: TrustMartPool
      maximum-pool-size: 20
      minimum-idle: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        